# GovLink 知识库维基 (Knowledge Wiki)

> 基于 Karpathy 的 LLM Wiki 方法论构建的个人知识库。
> 核心理念：**LLM 负责维护，你负责思考**。

---

## 核心理念

传统 RAG 的问题：每次提问，LLM 从零发现知识，没有积累。

我们的方案：让 LLM **增量构建并维护一个持久的 wiki**。新资料进来时，LLM 读取、提取、整合——更新实体页、修订摘要、标记矛盾、强化联系。知识编译一次，**持续保持最新**，不再每次重复推导。

关键区别：**wiki 是一个持久、复利的知识制品**。交叉引用已就位，矛盾已标记，综合已反映所有读过的东西。

---

## 三层架构

### 1. Raw Sources（原始资料层）
- **位置**: `raw/sources/`
- **作用**: 存放不可变的源文档。LLM 只读不写，这是你的真理来源。
- **内容类型**: 文章、PDF、网页剪藏、微信文章、图片、数据文件等
- **规则**: 原始资料永远是原始的，LLM 不修改它们

### 2. Wiki（维基层）
- **位置**: `wiki/` 及其子文件夹
- **作用**: LLM 生成和维护的结构化笔记。LLM 全权拥有这一层。
- **子文件夹**:
  - `entities/` — 实体页（人物、公司、产品、项目）
  - `concepts/` — 概念页（方法论、术语、原理）
  - `summaries/` — 摘要页（书籍摘要、文章摘要、会议纪要）


### 3. Schema（本文件）
- **位置**: `CLAUDE.md`
- **作用**: 告诉 LLM wiki 的结构、约定、以及工作流程
- **维护**: 你和 LLM 共同演进，随着领域理解加深不断完善

---

## 文件约定

### Wiki 页面命名
- 使用简洁的中文标题，如 `大美政通-组织管理.md`
- 实体页以实体名称开头，如 `大美政通.md`
- 概念页用简洁术语，如 `OKR管理.md`

### Wiki 页面结构
```markdown
---
title: 页面标题
tags: [entity, company]
created: 2026-04-29
sources: 1
---

# 页面标题

## 概述
简短介绍。

## 关键信息
整理的关键点。

## 相关资料
- [[wiki/summaries/某书摘要]]
- [[raw/sources/原始文章]]

## 最近更新
- [2026-04-29] 创建页面
```

---

## 工作流程

### Ingest（摄入新资料）

当你有新资料要处理时：

1. 将资料放入 `raw/sources/` 或其子分类
2. 告诉 LLM：`请处理这个资料，更新维基`
3. LLM 会：
   - 读取源资料
   - 与你讨论关键要点
   - 在 wiki 中写摘要页
   - 更新 index.md
   - 更新相关实体和概念页
   - 在 log.md 中记录

**单次摄入可能涉及 10-15 个 wiki 页面**。

### Query（查询）

当你想查询知识时：

1. 直接提问，如：`关于公司组织管理有什么沉淀？`
2. LLM 会：
   - 搜索 wiki 中的相关页面
   - 阅读并综合答案
   - 给出带引用的回复
3. **重要**：如果回复有价值，让 LLM 将其保存为 wiki 页面

### Lint（健康检查）

定期（每周或每月）让 LLM 检查 wiki 健康度：

- 页面之间的矛盾
- 被新资料超越的旧观点
- 没有入口链接的孤立页面
- 有提及但没有自己页面的重要概念
- 缺失的交叉引用
- 可以用网络搜索填补的数据空白

---

## 特殊文件

### index.md（维基目录）
- **位置**: `wiki/index.md`
- **作用**: 所有 wiki 页面的目录，每个页面有一行摘要
- **格式**: 按类别组织（实体、概念、摘要等）
- **维护**: 每次 ingest 后更新

### log.md（活动日志）
- **位置**: `维基/log.md`
- **作用**: 按时间顺序记录所有活动（摄入、查询、lint）
- **格式**: 每条记录以 `## [日期] 类型 | 标题` 开头
- **用途**: 追踪 wiki 演化历史，帮助 LLM 理解最近做了什么

**快速查看最近活动**:
```bash
grep "^## \[" wiki/log.md | tail -5
```

---

## 文件夹结构总览

```
GovLink的仓库/
├── CLAUDE.md                 # Schema（当前文件）
├── .claude/                  # Claude 相关配置
├── .github/                  # GitHub 配置
│   └── workflows/            # GitHub Actions
├── raw/                      # Raw Sources（原始资料层）
│   ├── sources/             # 通用来源
│   │   ├── articles/        # 文章剪藏
│   │   ├── books/           # 书籍
│   │   ├── podcasts/        # 播客
│   │   ├── reports/         # 报告
│   │   └── 项目部/          # 公司项目部原始资料
│   │       ├── 历史积累/    # 历史沉淀数据
│   │       ├── 技能/        # 操作技能文档
│   │       └── 知识/        # 知识类文档
│   └── assets/              # 原始资料中的图片/附件
├── wiki/                     # Wiki（维基层）
│   ├── index.md             # 维基目录
│   ├── log.md               # 活动日志
│   ├── entities/            # 实体页
│   ├── concepts/            # 概念页
│   └── summaries/           # 摘要页（SOP、模板等）
```

---

## GovLink 公司背景

### 基本信息
- **公司名称**: 大美政通 / GovLink
- **全称**: 上海大美国际旅行社有限公司、上海鲸杉商务咨询有限公司
- **业务**: 商务旅行服务，特别是政府公务团组出访
- **目标市场**: 全球

### 核心业务流程
公司有完整的 SOP 文档，已同步到 `wiki/summaries/`：
- **项目操作指南**: 团组出访全流程 SOP
- **项目文件**: 各阶段所需文件模板


### 关键原则
- 三公经费合规
- 风险控制（税务、法务）
- 以人为本、无限进步

---

## GitHub 自动同步

每天自动将整个 vault 同步至 [GovLink-PM](https://github.com/Sweid-Tim/GovLink-PM) 仓库。

### 同步方式
- **本地定时任务**: macOS launchd 每天北京时间 8:00 自动 commit + push
- **同步范围**: 整个 vault（含 raw/、wiki/、笔记文件夹等）
- **认证方式**: SSH Key

### 本地脚本
同步脚本位于 `.claude/sync-to-github.sh`，内容包括：
1. `git add -A` 暂存所有变更
2. `git commit`（若无变更则跳过）
3. `git push` 推送到 GitHub

### Git 配置
- `.gitignore` 排除 `.obsidian/`、`.DS_Store`、`.claudian/sessions/` 等本地配置

---

## 工具推荐

- **Obsidian Web Clipper**: 浏览器插件，将网页转换为 markdown
- **图片本地化**: 设置 → 文件和链接 → 附件文件夹路径设为 `raw/assets/`，然后用热键 `Ctrl+Shift+D` 下载图片
- **Graph View**: 查看 wiki 的结构——哪些页面互联、哪些是孤立页面
- **Dataview**: 用 frontmatter 生成动态表格和列表
- **Marp**: 从 wiki 内容生成演示文稿
- **Claudian**: Obsidian 的 AI 协作者插件，直接在 vault 内运行 Claude

---

## 为什么这有效

维护知识库最繁琐的不是阅读或思考，而是 ** bookkeeping（记账式工作）**：
- 更新交叉引用
- 保持摘要最新
- 标记矛盾
- 维护一致性

人类放弃 wiki 是因为维护负担增长快于价值。LLM 不会厌倦、不会忘记更新交叉引用、一次可以修改 15 个文件。

**人的工作是**：策划来源、引导分析、问好问题、思考意义。

**LLM 的工作是**：其他一切。

---

*本文档基于 [Karpathy LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) 方法论，由 Claude 帮助构建。*
